import React from 'react';
import { Grid } from '@material-ui/core';
import {ImagesProps} from "../../Interfaces/Images";

export const Images: React.FunctionComponent<ImagesProps> = ({ images }) => {
    return (
        <Grid container spacing={2} justify={"center"}>

        </Grid>
    );
}
